import {sum, mag} from '../basic';

test('should sum', () => {
  const result = sum([1, 2, 3]);

  expect(result).toBe(6);
});

//import mag from '../basic';

test('should mag', () => {
  const result = healthMag;

  expect(result).toBe('healthy');
});